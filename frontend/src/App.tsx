import {FormEvent,useEffect,useState} from 'react';import{Activity,BookOpen,ChevronRight,Globe2,LogOut,Menu,Moon,Network,Play,RefreshCw,Server,Settings2,ShieldCheck,Square,Sun,Users,X}from'lucide-react';
type Lang='zh-CN'|'en';type Item={id:number;name:string;enabled?:boolean;[k:string]:any};
const text={
'zh-CN':{overview:'概览',subs:'订阅',nodes:'节点',accounts:'Socks 账号',config:'Mihomo 配置',refresh:'刷新',start:'启动',stop:'停止',logout:'退出',running:'运行中',stopped:'已停止',port:'SOCKS5 端口',addSub:'添加并同步',url:'订阅 URL',name:'名称',optional:'可选',sync:'同步',del:'删除',edit:'编辑',enabled:'启用',type:'类型',server:'服务器',subscription:'订阅',noSubscription:'未关联订阅',unknownSubscription:'未知订阅',user:'用户名',password:'密码',node:'节点',save:'保存',cancel:'取消',preview:'预览配置',empty:'暂无数据',setup:'初始化管理器',setupDesc:'设置管理员账号以开始使用。',login:'管理员登录',loginDesc:'输入管理员账号和密码。',loginFailed:'登录失败：',setupFailed:'初始化失败：',admin:'管理员用户',welcome:'代理服务控制台',status:'服务状态',quick:'集中管理订阅、节点与访问账号',lastSync:'最近同步',signIn:'登录'},
en:{overview:'Overview',subs:'Subscriptions',nodes:'Nodes',accounts:'Socks Accounts',config:'Mihomo Config',refresh:'Refresh',start:'Start',stop:'Stop',logout:'Sign out',running:'Running',stopped:'Stopped',port:'SOCKS5 Port',addSub:'Add & Sync',url:'Subscription URL',name:'Name',optional:'Optional',sync:'Sync',del:'Delete',edit:'Edit',enabled:'Enabled',type:'Type',server:'Server',subscription:'Subscription',noSubscription:'No subscription',unknownSubscription:'Unknown subscription',user:'Username',password:'Password',node:'Node',save:'Save',cancel:'Cancel',preview:'Preview config',empty:'No data yet',setup:'Initialize manager',setupDesc:'Set an administrator account to get started.',login:'Administrator sign in',loginDesc:'Enter your administrator credentials.',loginFailed:'Sign-in failed: ',setupFailed:'Initialization failed: ',admin:'Admin user',welcome:'Proxy service console',status:'Service status',quick:'Manage subscriptions, nodes and access accounts',lastSync:'Last sync',signIn:'Sign in'}};
async function api(path:string,options:RequestInit={}){const r=await fetch(path,{...options,headers:{'content-type':'application/json',...options.headers}});const raw=await r.text();let data:any=null;try{data=raw?JSON.parse(raw):null}catch{data=raw}if(!r.ok){const message=typeof data?.error==='string'&&data.error.trim()?data.error:typeof data==='string'&&data.trim()?data:`Request failed (${r.status}${r.statusText?` ${r.statusText}`:''})`;throw new Error(message)}return data}const body=(v:any)=>({method:'POST',body:JSON.stringify(v)});
export default function App(){const[lang,setLang]=useState<Lang>('zh-CN'),t=text[lang];const[dark,setDark]=useState(()=>localStorage.theme!=='light');const[mobile,setMobile]=useState(false);const[view,setView]=useState<'loading'|'setup'|'login'|'app'>('loading');const[active,setActive]=useState('overview');const[subs,setSubs]=useState<Item[]>([]),[nodes,setNodes]=useState<Item[]>([]),[accounts,setAccounts]=useState<Item[]>([]),[editing,setEditing]=useState<Item|null>(null);const[mihomo,setMihomo]=useState<any>({}),[port,setPort]=useState('-'),[toast,setToast]=useState(''),[authError,setAuthError]=useState(''),[busy,setBusy]=useState(false),[yaml,setYaml]=useState('');
useEffect(()=>{document.documentElement.classList.toggle('dark',dark);localStorage.theme=dark?'dark':'light'},[dark]);const notify=(s:string)=>{setToast(s);setTimeout(()=>setToast(''),2600)};async function load(){const s=await api('/api/status');setMihomo(s.mihomo||{});setPort(s.socks_port||'-');if(!s.initialized)return setView('setup');if(!s.authenticated)return setView('login');const[a,b,c]=await Promise.all([api('/api/subscriptions'),api('/api/nodes'),api('/api/socks-accounts')]);setSubs(a);setNodes(b.nodes||[]);setAccounts(c);setView('app')}useEffect(()=>{load().catch(e=>{notify(e.message);setView('login')})},[]);async function action(fn:()=>Promise<void>){setBusy(true);try{await fn()}catch(e:any){notify(e.message)}finally{setBusy(false)}}
const auth=async(e:FormEvent<HTMLFormElement>,path:string)=>{e.preventDefault();setAuthError('');setBusy(true);try{await api(path,body(Object.fromEntries(new FormData(e.currentTarget))))}catch(e:any){const prefix=view==='setup'?t.setupFailed:t.loginFailed;setAuthError(prefix+(e instanceof Error?e.message:String(e)));setBusy(false);return}setView('app');try{await load()}catch(e:any){notify(e instanceof Error?e.message:String(e))}finally{setBusy(false)}};const nav=[['overview',Activity,t.overview],['subscriptions',BookOpen,t.subs],['nodes',Network,t.nodes],['accounts',Users,t.accounts],['config',Settings2,t.config]] as const;
if(view==='loading')return <div className="splash"><span className="loader"/></div>;if(view!=='app')return <div className="auth"><div className="auth-art"><div className="brand"><ShieldCheck/> Rust Proxy</div><div><span className="eyebrow">SUB2SOCKS</span><h1>{t.quick}</h1><p>{t.welcome}</p></div></div><div className="auth-panel"><div className="auth-tools"><button onClick={()=>setDark(!dark)}>{dark?<Sun/>:<Moon/>}</button><button onClick={()=>setLang(lang==='en'?'zh-CN':'en')}><Globe2/>{lang==='en'?'中':'EN'}</button></div><form className="auth-card" onSubmit={e=>auth(e,view==='setup'?'/api/init':'/api/auth/login')}><div className="auth-icon"><ShieldCheck/></div><h2>{view==='setup'?t.setup:t.login}</h2><p>{view==='setup'?t.setupDesc:t.loginDesc}</p><label>{t.admin}<input name="admin_user" required autoComplete="username"/></label><label>{t.password}<input name="admin_pass" type="password" required autoComplete={view==='setup'?'new-password':'current-password'} aria-describedby={authError?'auth-error':undefined}/></label>{authError&&<p id="auth-error" className="auth-error" role="alert">{authError}</p>}<button className="primary" disabled={busy}>{view==='setup'?t.setup:t.signIn}<ChevronRight/></button></form></div>{toast&&<div className="toast">{toast}</div>}</div>;
const mutate=async(path:string,options:RequestInit,message:string)=>action(async()=>{await api(path,options);await load();notify(message)});return <div className="shell"><aside className={mobile?'open':''}><div className="brand"><ShieldCheck/> <span>Rust Proxy</span><button className="close" onClick={()=>setMobile(false)}><X/></button></div><nav>{nav.map(([id,I,label])=><button className={active===id?'active':''} onClick={()=>{setActive(id);setMobile(false)}} key={id}><I/>{label}</button>)}</nav><div className="aside-foot"><div className={'status-dot '+(mihomo.running?'on':'')}/><div><strong>{t.status}</strong><span>{mihomo.running?t.running:t.stopped}</span></div></div></aside>{mobile&&<div className="scrim" onClick={()=>setMobile(false)}/>}<main><header><button className="hamb" onClick={()=>setMobile(true)}><Menu/></button><div><span className="eyebrow">CONTROL CENTER</span><h1>{nav.find(n=>n[0]===active)?.[2]}</h1></div><div className="header-actions"><button onClick={()=>setLang(lang==='en'?'zh-CN':'en')}><Globe2/><span>{lang==='en'?'中':'EN'}</span></button><button onClick={()=>setDark(!dark)}>{dark?<Sun/>:<Moon/>}</button><button onClick={()=>action(load)}><RefreshCw/></button><button onClick={()=>action(async()=>{try{await api('/api/auth/logout',body({}))}finally{setView('login')}})}><LogOut/></button></div></header><div className="content">
{active==='overview'&&<><div className="hero"><div><span className="eyebrow">MIHOMO CORE</span><h2>{t.welcome}</h2><p>{t.quick}</p></div><div className="hero-actions"><button className="primary" onClick={()=>mutate('/api/mihomo/start',body({}),'Mihomo started')}><Play/>{t.start}</button><button onClick={()=>mutate('/api/mihomo/stop',body({}),'Mihomo stopped')}><Square/>{t.stop}</button></div></div><div className="stats"><Stat icon={<Activity/>} label={t.status} value={mihomo.running?t.running:t.stopped}/><Stat icon={<Server/>} label={t.port} value={String(port)}/><Stat icon={<BookOpen/>} label={t.subs} value={String(subs.length)}/><Stat icon={<Network/>} label={t.nodes} value={String(nodes.length)}/></div></>}
{active==='subscriptions'&&<Section title={t.subs} count={subs.length}><form className="form-row" onSubmit={async e=>{e.preventDefault();await mutate('/api/subscriptions',body(Object.fromEntries(new FormData(e.currentTarget))),t.addSub);e.currentTarget.reset()}}><label>{t.url}<input name="url" type="url" required placeholder="https://example.com/sub"/></label><label>{t.name}<input name="name" placeholder={t.optional}/></label><button className="primary">{t.addSub}</button></form><Table heads={[t.name,t.url,t.lastSync,'']} rows={subs.map(s=>[s.name,s.url,s.last_synced_at?new Date(s.last_synced_at*1000).toLocaleString():'-',<Actions sync={()=>mutate(`/api/subscriptions/${s.id}/sync`,body({}),t.sync)} del={()=>mutate(`/api/subscriptions/${s.id}`,{method:'DELETE'},t.del)} t={t}/>])} empty={t.empty}/></Section>}
{active==='nodes'&&<Section title={t.nodes} count={nodes.length}><Table heads={[t.enabled,t.name,t.type,t.server,t.subscription,'']} rows={nodes.map(n=>{const subscriptionId=Number(n.subscription_id);const subscription=Number.isFinite(subscriptionId)&&subscriptionId>0?subs.find(s=>s.id===subscriptionId):undefined;const subscriptionLabel=subscription?.name||(n.subscription_id==null||n.subscription_id===''||!Number.isFinite(subscriptionId)||subscriptionId<=0?t.noSubscription:t.unknownSubscription);return [<input type="checkbox" checked={!!n.enabled} onChange={e=>mutate(`/api/nodes/${n.id}/enabled`,{method:'PUT',body:JSON.stringify({enabled:e.target.checked})},t.enabled)}/>,n.name,n.node_type,`${n.server||''}:${n.port||''}`,subscriptionLabel,<button className="danger" onClick={()=>mutate(`/api/nodes/${n.id}`,{method:'DELETE'},t.del)}>{t.del}</button>]})} empty={t.empty}/></Section>}
{active==='accounts'&&<Section title={t.accounts} count={accounts.length}><AccountForm t={t} nodes={nodes} editing={editing} cancel={()=>setEditing(null)} save={async(v:any,id:string)=>{await mutate(id?`/api/socks-accounts/${id}`:'/api/socks-accounts',{method:id?'PUT':'POST',body:JSON.stringify(v)},t.save);setEditing(null)}}/><Table heads={[t.enabled,t.name,t.user,t.node,'']} rows={accounts.map(a=>[<input type="checkbox" checked={!!a.enabled} onChange={e=>mutate(`/api/socks-accounts/${a.id}/enabled`,{method:'PUT',body:JSON.stringify({enabled:e.target.checked})},t.enabled)}/>,a.name,a.username,nodes.find(n=>n.id===a.node_id)?.name||a.node_id,<div className="actions"><button onClick={()=>setEditing(a)}>{t.edit}</button><button className="danger" onClick={()=>mutate(`/api/socks-accounts/${a.id}`,{method:'DELETE'},t.del)}>{t.del}</button></div>])} empty={t.empty}/></Section>}
{active==='config'&&<Section title={t.config}><button className="primary" onClick={()=>action(async()=>setYaml((await api('/api/config/preview')).yaml))}>{t.preview}</button><pre>{yaml||'# '+t.preview}</pre></Section>}</div></main>{busy&&<div className="loading"><span className="loader"/></div>}{toast&&<div className="toast">{toast}</div>}</div>}
function Stat(p:any){return <div className="stat"><i>{p.icon}</i><div><span>{p.label}</span><strong>{p.value}</strong></div></div>}function Section(p:any){return <section className="card"><div className="section-title"><h2>{p.title}</h2>{p.count!==undefined&&<span>{p.count}</span>}</div>{p.children}</section>}function Table({heads,rows,empty}:any){return <div className="table-wrap">{rows.length?<table><thead><tr>{heads.map((h:string,i:number)=><th key={i}>{h}</th>)}</tr></thead><tbody>{rows.map((r:any[],i:number)=><tr key={i}>{r.map((v,j)=><td key={j}>{v}</td>)}</tr>)}</tbody></table>:<div className="empty">{empty}</div>}</div>}function Actions({sync,del,t}:any){return <div className="actions"><button onClick={sync}>{t.sync}</button><button className="danger" onClick={del}>{t.del}</button></div>}
function AccountForm({t,nodes,save,editing,cancel}:any){return <form key={editing?.id||'new'} className="account-grid" onSubmit={async e=>{e.preventDefault();const f:any=Object.fromEntries(new FormData(e.currentTarget));f.node_id=Number(f.node_id);await save(f,editing?.id?String(editing.id):'');e.currentTarget.reset()}}><label>{t.name}<input name="name" defaultValue={editing?.name||''} required/></label><label>{t.user}<input name="username" defaultValue={editing?.username||''} required/></label><label>{t.password}<input name="password" type="password" defaultValue={editing?.password||''} required/></label><label>{t.node}<select name="node_id" defaultValue={editing?.node_id} required>{nodes.filter((n:any)=>n.enabled||n.id===editing?.node_id).map((n:any)=><option value={n.id} key={n.id}>{n.name} ({n.node_type})</option>)}</select></label><button className="primary">{t.save}</button>{editing&&<button type="button" onClick={cancel}>{t.cancel}</button>}</form>}
